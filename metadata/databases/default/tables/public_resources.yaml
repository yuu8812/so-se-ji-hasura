table:
  name: resources
  schema: public
array_relationships:
  - name: resource_editable_users
    using:
      foreign_key_constraint_on:
        column: resource_id
        table:
          name: resource_editable_users
          schema: public
  - name: resource_histories
    using:
      foreign_key_constraint_on:
        column: resource_id
        table:
          name: resource_histories
          schema: public
